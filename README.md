# OTPFloodGuard Benchmark

This repository contains the code and generated files used for the paper:

**OTPFloodGuard: A Public-Evidence-Constrained Benchmark for Lightweight OTP Flooding Detection**

Authors: Wenche An and Kamran Aziz

## What is included

- `src/run_otpfloodguard_experiment.py`  
  Python script used to generate the simulated benchmark, train baseline models, run stress tests, and export the result files.
- `data/`  
  Simulated OTP request-window data generated by the script.
- `results/`  
  CSV files used for the paper tables and reported metrics.
- `figures/`  
  Figures used in the paper.

## Run a quick check

From the repository root:

```bash
pip install -r requirements.txt
python3 src/run_otpfloodguard_experiment.py --quick
```

This creates a fast seed-42 smoke-test output in `results/quick_metrics.csv`.

## Reproduce the paper results

To regenerate the full simulated benchmark results:

```bash
python3 src/run_otpfloodguard_experiment.py
```

This writes the simulated dataset, result CSV files, and figures to `data/`, `results/`, and `figures/`.

The full run may take several minutes because it includes multi-seed evaluation, generator-shift testing, attack-intensity analysis, feature-group ablation, threshold analysis, and diagnostic figures.

## Result files

- `results/metrics.csv` -- main Overlap benchmark results
- `results/multi_seed_summary.csv` -- seven-seed stability results
- `results/cross_generator_metrics.csv` -- generator-shift robustness results
- `results/difficulty_metrics.csv` -- Easy, Overlap, and Adaptive results
- `results/attack_intensity_metrics.csv` -- attack-intensity stress-test results
- `results/ablation_metrics.csv` -- feature-group ablation results
- `results/cost_sensitive_thresholds.csv` -- threshold results under relative FN/FP costs
- `results/error_analysis_summary.csv` -- representative false-positive and false-negative patterns
- `results/feature_dictionary.csv` -- feature definitions
- `results/test_predictions.csv` -- seed-42 test predictions used for threshold and error analysis

## Notes

The data in this repository is simulated. It does not contain real OTP logs, real phone numbers, private user records, or production authentication data.

The reported scores should be read as benchmark results under the stated simulation assumptions, not as real-world OTP flooding detection accuracy.
